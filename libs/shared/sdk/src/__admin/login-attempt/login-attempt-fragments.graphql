fragment __AdminLoginAttemptSummary on LoginAttempt {
  id
  createdAt
  updatedAt
  userId
  email
  success
  ipAddress
  userAgent
  location
  reason
  user {
    id
  }
}

fragment __AdminLoginAttemptDetails on LoginAttempt {
  ...__AdminLoginAttemptSummary
}
